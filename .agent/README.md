# .agent

Shared working directory for every agent harness.

`worktrees/` holds per-branch git worktrees and is gitignored. `skills/` holds
reusable task instructions.

Conventions live in `AGENTS.md` at the repository root, not here.
